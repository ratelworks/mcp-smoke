# Changelog

## v0.1.0

- Initial release of the mcp-smoke CLI.
- Adds config parsing for `mcpServers`, `servers`, and single server objects.
- Adds read-only smoke checks for commands, cwd, env, scripts, and remote URLs.
